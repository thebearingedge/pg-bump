require('dotenv/config')
const { expect } = require('chai')
const { describe, it, beforeEach, afterEach } = require('mocha')

module.exports = {
  it,
  expect,
  describe,
  afterEach,
  beforeEach
}

language: node_js
node_js:
  - 6.10.3
services: postgresql
before_script:
  - psql -c 'create database "pg-bump";' -U postgres
  - export DATABASE_URL=postgres://127.0.0.1:5432/pg-bump
install:
  - npm install
  - npm install -g codecov
script:
  - npm run cover
  - codecov
